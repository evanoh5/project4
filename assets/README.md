# Assets

Drop your profile photo here as `profile.jpg` (square crop recommended,
at least 520×520px). The site renders it grayscale automatically to match
the theme, and shows a neutral placeholder until the file exists.
